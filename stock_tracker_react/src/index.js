import React from 'react';
import ReactDOM from 'react-dom/client';
import TerminalComponent from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TerminalComponent/>
  </React.StrictMode>
);

